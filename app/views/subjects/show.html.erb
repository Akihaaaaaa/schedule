<div class="shousai">
<h1>気になる勉強法の詳細</h1>
<div class="subject">
  <p>教科名:<%= @subjects.name %></p>
  <p>内容:<%= @subjects.contents %></p>
  <p>学年:<%= @subjects.grade %></p>
  <%= image_tag @subjects.image_url, size: "250x200" if @subjects.image? %>
</div>


<div class="comment-wrapper">
  <h2>みんなのコメントと評価</h2>
  <% @comments.each do |c| %>
    <div>
      <%= c.user.email unless c.user.blank? %>
      <br>
      <%= c.content %>

      <div>
<span class="star-rating">
   <span class="star-rating-front" style="width: <%= getPercent(c.level) %>%;">★★★★★</span>
   <span class="star-rating-back">★★★★★</span>
</span>
</h2>
</div>
      
      
      

    
  <% end %>

  <h2>この勉強法に評価とコメントをつける</h2>

  <% if user_signed_in? %>
    <%= form_with(model: [@subjects, @comment], local: true) do |f| %>

    
      <div class="post_form">
        <%= f.radio_button :level,  5 ,id: 'star6'%>
        <label for="star6"><span class="text">すごく良い</span>★</label>
    
        <%= f.radio_button :level,  4 ,id: 'star7'%>
        <label for="star7"><span class="text">良い</span>★</label>
    
        <%= f.radio_button :level, 3 ,id: 'star8'%>
        <label for="star8"><span class="text">普通</span>★</label>
        <%= f.radio_button :overall, 2 ,id: 'star4'%>
    <label for="star4"><span class="text">悪い</span>★</label>
    
    <%= f.radio_button :overall, 1 ,id: 'star5'%>
    <label for="star5"><span class="text">最悪</span>★</label>
    </div>

      <%= f.text_area :content %>
      <%= button_tag type: "submit" do %>
        <i class="far fa-comments"></i> コメントする
      <% end %>
    <% end %>
  <% end %>
</div>





<%= link_to "一覧に戻る", subjects_path %>



</div>